'use strict';
module.exports = function(grunt) {

  grunt.initConfig({

    // watch for changes and trigger livereload
    watch: {
      },
      options: {
        livereload: true
      }
    },

    // load plugins

    // register tasks

};
